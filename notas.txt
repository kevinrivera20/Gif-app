Temas puntuales de la sección
¿Qué veremos en esta sección?

	-Custom Hooks

	-Fetch hacia un API

	-Comunicación entre componentes

	-Clases de CSS

	-Animaciones

	-Enviar métodos como argumentos

	-Crear listados

	-keys

	-Giphy

Esta es una aplicación pequeña pero muy ilustrativa que explica cómo utilizar React + customHooks para poder resolver necesidades en específico que podremos re-utilizar después.


API KEY, jjWNnxm5PiSeJhTPQIZpOVd56Nzhldqg.

Hooks.
	- Los hooks trabajan en react basados en su posición o ID, se manejan diferentes estados, pero todos se manejan posicionalmente.


UseEffect.
	- useEffect puede hacer uso de lo que se conoce como "función de limpieza", que es una función que se ejecuta cuando el componente se desmonta, pero también cuando las dependencias cambian y antes de que el efecto se vuelva a ejecutar, o si es un useEffect sin array de dependencias, en cada rerender, sirve para disparar efectos secundarios, algún proceso que querramos ejecutar cuando algo suceda. 


Custom Hook.
	-
